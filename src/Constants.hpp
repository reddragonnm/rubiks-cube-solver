#pragma once

namespace Constants {
    inline constexpr int windowWidth{ 800 };
    inline constexpr int windowHeight{ 600 };

    inline constexpr float cameraZ{ 20.f };
    inline constexpr float viewportZ{ 10.f };

    inline constexpr float rotationSpeed{ .1f };

    inline constexpr int numSides{ 3 };
    inline constexpr float cubeSize{ 2.f };
}
