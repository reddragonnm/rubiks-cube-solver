#pragma once

#include <array>
#include <unordered_set>
#include <vector>
#include <bitset>

#include "Cube.hpp"

class Solver3x3 {


public:
    Solver3x3() {}

    std::vector<char> solve(const Cube& cube) {
        std::vector<char> solution;
        return solution;
    }
};
